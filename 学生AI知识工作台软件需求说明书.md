# 学生AI知识工作台软件需求说明书

## 1 引言

### 1.1 项目背景

在日常学生学习场景中，存在学习资料零散、文档查阅繁琐、知识无法快速检索答疑、学习笔记管理混乱、小组学习资源难以共享等问题。传统的本地文档、记事本工具功能单一，无法满足学生智能化、轻量化的知识管理需求。

本项目基于 Vue3 + SpringBoot 技术栈，结合 Dify 智能工作流能力，搭建一款轻量化**学生个人 AI 知识工作台**。聚焦学生个人知识管理与小团队资源协作，通过接入 Dify API 实现复杂的文档解析、向量索引、智能问答能力，业务层仅实现基础 CRUD、用户权限、文件管理、笔记管理、团队共享等简易功能，适配期末项目开发难度，同时满足实际学习使用场景。

### 1.2 项目目的

本系统旨在为学生提供一站式知识管理工具，实现学习文档规范化管理、AI 智能答疑、Markdown 笔记记录、小团队知识库共享功能。通过解耦复杂 AI 能力与业务开发，降低项目开发难度，仅通过后端转发 API 调用智能能力，业务侧专注基础功能开发，兼顾项目完整性与落地可行性。

### 1.3 适用范围

本文档为项目开发唯一需求依据，适用于前端 Vue3 开发、后端 SpringBoot 开发、数据库设计、功能测试及项目答辩提交。系统仅适配 PC 端浏览器使用，面向个人学生用户及小型学习团队。

### 1.4 技术栈架构

- **前端**：Vue3 + 开源 Markdown 编辑器 + 主题切换插件 + 第三方文件预览组件（PDF / Word / Markdown 只读预览）

- **后端**：SpringBoot + MySQL + JWT 认证

- **对象存储**：MinIO（部署于本地 Docker），用于头像、学习文件等二进制资源存储；**不接入**阿里云 OSS、腾讯云 COS 等公有云存储

- **AI 核心能力**：基于 Dify 工作流实现文档解析、向量索引、跨文档智能问答，后端通过 API 转发调用，不实现任何 AI 算法逻辑

- **权限体系**：极简 RBAC 权限控制

> **【注意】** 文件「本地存储」在本项目中指自建 MinIO 服务（Docker 部署），非服务器磁盘直存路径；数据库中 `file_path`、`avatar` 等字段存储 MinIO 的 **object key** 或可访问 URL，而非操作系统绝对路径。

## 2 总体概述

### 2.1 用户角色

系统设置两类固定角色，通过 `user_role` 关联表实现，**同一用户可同时拥有两个角色**：

| 角色 | 说明 |
|------|------|
| **普通用户** | 注册账号时默认分配。可使用个人文件管理、AI 问答、笔记管理、接受团队邀请、使用团队共享知识库 |
| **团队创建者** | 用户**首次创建团队**后自动追加该角色；若已拥有则不再重复插入。表示该用户具备创建/管理团队的资格标识 |

**权限判定规则（重要）：**

- **全局角色**（`user_role`）：用于身份展示与粗粒度能力判断（如是否展示「创建团队」相关入口）
- **团队管理权**：必须校验 `team.creator_id = 当前用户 ID`，与是否拥有「团队创建者」角色无关——用户 A 即使拥有团队创建者角色，也**不能**管理用户 B 创建的团队
- **同一人可创建并管理多个团队**，每个团队的 `creator_id` 均为该用户 ID

> **【注意】** 「团队创建者」是全局身份标签，不是「所有团队的管理员」。具体某个团队的管理操作，始终以 `team.creator_id` 为准。

### 2.2 核心产品定位

轻量化学生专属 AI 知识协作工作台，核心能力聚焦三点：个人文档智能化问答、Markdown 学习笔记管理、小团队知识库极简共享。所有复杂 AI 逻辑依托 Dify 实现，自研业务仅为基础增删改查、权限控制、MinIO 文件存储与 API 转发。

### 2.3 核心业务流程

1. 用户注册登录系统（JWT 鉴权），完善个人头像（MinIO）、主题偏好等个人配置；

2. 用户创建知识库分类（`file_category`），上传 PDF、Word、Markdown 格式文件，文件存入 MinIO；

3. 用户手动触发「同步知识库」操作，后端调用 Dify API 完成文档解析、向量索引；同步结果以三态字段展示；

4. 用户选择单个/多个知识库分类，发起 AI 问答，后端转发请求至 Dify，返回结果并保存对话记录；

5. 用户创建 Markdown 学习笔记，通过多标签分类管理，支持模糊筛选；

6. 团队创建者创建团队、邀请成员；开启共享后，团队成员可预览、问答创建者**名下全部**知识库分类下的已同步文件。

## 3 功能性需求

本系统功能分为**六大模块**，严格区分**自研开发功能**与 **Dify API 调用功能**，杜绝复杂自研逻辑，贴合期末开发需求。

### 3.1 用户账号与个人中心模块（全自研）

#### 3.1.1 登录注册功能

- 支持**用户名 + 密码**注册、登录，无手机号、邮箱验证

- 数据校验：
  - 用户名唯一、非空
  - 密码非空，长度不少于 6 位（期末项目从简，不做复杂度、验证码等要求）
  - 注册时确认密码一致

- 登录认证采用 **JWT**：
  - 登录成功返回 `accessToken` 及用户基本信息
  - 前端请求头携带 `Authorization: Bearer <token>`
  - Token 有效期建议 7 天；首版可不实现 Refresh Token

- 密码使用 BCrypt 等方式加密存储，禁止明文保存

> **【注意】** JWT Payload 建议包含 `userId`、`username`，可选携带角色列表 `roles`，便于前端展示；团队级权限仍须后端按业务规则二次校验。

#### 3.1.2 个人中心功能

- 账号设置：支持修改登录密码（需校验旧密码）、上传/更换个人头像

- 头像存储：上传至 MinIO，路径建议 `avatars/{userId}/{uuid}.{ext}`，`user.avatar` 存 object key 或访问 URL

- 主题设置：支持白天模式、夜间模式、跟随系统三种主题切换

### 3.2 权限管理模块（全自研，极简 RBAC）

- 角色固定为「普通用户」「团队创建者」两类，不可自定义新增角色

- 用户注册时仅分配「普通用户」；首次创建团队后追加「团队创建者」角色

- 团队创建者仅对 **`creator_id` 为自己的团队** 拥有管理权限，无全局系统管理员

- 团队资源权限：成员仅可读（在线预览）、可 AI 问答，不可修改、删除、上传文件及知识库分类

- 禁止越权访问他人个人文件、笔记、未加入团队的数据

### 3.3 文件知识库管理模块（自研 + Dify 联动）

#### 3.3.1 基础文件管理（自研）

- 支持上传文件格式：Markdown、PDF、Word（doc/docx）

- 文件操作：新增、删除、查询、重命名；支持 PC 端在线预览，**不支持文件在线编辑**

- 文件存储：上传至 MinIO，路径建议 `files/{userId}/{categoryId}/{uuid}_{originalName}`

- 知识库分类：支持自定义创建、删除、重命名文件夹（即 `file_category`），实现文件归类管理

- 文件预览：采用第三方前端组件实现只读预览，建议方案：
  - PDF：`vue-pdf-embed` 或 `@vue-office/pdf`
  - Word：`@vue-office/docx`
  - Markdown：编辑器只读模式或 Markdown 渲染组件

- 文件访问：后端提供鉴权后的下载/预签名 URL，避免 MinIO Bucket 完全公开

> **【注意】** 删除文件时，需同步删除 MinIO 对象；若该文件已成功同步 Dify，还需调用 Dify API 删除对应 Document（见 3.3.2）。重命名仅更新本地 `file_name`，一般无需重新同步 Dify，除非 Dify 侧需展示文件名。

#### 3.3.2 AI 索引同步（Dify API 调用）

- 文件上传至 MinIO 后，**不会自动同步**；需用户手动点击「同步知识库」按钮触发

- 同步范围：**按当前选中的单个知识库分类**同步该分类下全部待同步文件（`sync_status` 为 `0` 或 `2`）；不支持一键同步全部分类

- 交互说明：用户在某一分类页面/选中某一分类后，点击「同步知识库」即仅处理该 `category_id` 下的文件

- 后端接收指令，调用 Dify API 完成文档解析、内容提取、向量索引入库

- 同步状态字段 `sync_status`（三态）：
  - `0` = 未同步（上传后默认值）
  - `1` = 同步成功
  - `2` = 同步失败（允许用户再次点击同步重试）

- 同步成功后缓存 `dify_document_id`；同步失败时前端展示失败状态，便于用户感知

**Dify 与本地数据映射规则：**

| 本地实体 | Dify 侧实体 | 说明 |
|----------|-------------|------|
| 每个 `file_category` | 1 个 Dataset（知识库） | 首次同步该分类时创建并缓存 `dify_dataset_id` |
| 每个 `file` | Dataset 内 1 个 Document | 同步成功后缓存 `dify_document_id` |
| AI 问答 | Dify Chat / 工作流应用 | 多轮对话、RAG、Prompt 均在 Dify 配置 |

- 用户选择多个知识库分类问答时，后端将对应多个 `dify_dataset_id` 组装后转发至 Dify 工作流

> **【注意】** 文档分块策略、Embedding 模型、Prompt 模板等均在 Dify 控制台配置，后端不做算法实现。Dify 具体 API 路径、请求体格式以实现阶段对接文档为准，本需求仅约定映射关系与转发职责。

### 3.4 AI 智能问答模块（转发 API + 自研记录 CRUD）

#### 3.4.1 问答核心能力（Dify API 调用）

- 支持多轮连续对话，上下文语义承接由 Dify 工作流实现；通过 `conversation_id` 关联同一轮会话内的多轮提问

- **个人问答**：支持用户自定义选择**单个/多个**知识库分类作为数据源，选中即匹配该分类下全部已成功同步（`sync_status = 1`）的文档

- **团队共享问答（最简方案）**：成员在某一团队上下文中发起问答时，**无需手动选题**，后端自动使用该团队创建者名下**全部**已成功同步分类的 `dify_dataset_id` 列表转发 Dify
  - 前置校验：当前用户为该团队有效成员（`team_member.status = 已加入`）且 `team.is_share = 1`
  - 请求须携带 `team_id`，便于权限校验与历史记录区分

- 用户选择多个知识库分类（个人问答）时，后端将对应多个 `dify_dataset_id` 组装后转发至 Dify 工作流

**多轮对话与会话标识：**

| 场景 | 规则 |
|------|------|
| 新建会话 | 首轮问答不传 `conversation_id`，由 Dify 返回新会话 ID，后端写入 `chat_history` |
| 继续会话 | 前端携带已有 `conversation_id` 请求，后端原样转发 Dify，同一 `conversation_id` 下可有多条历史 |
| 历史存储 | **每一轮问答（一问一答）写入一条** `chat_history` 记录，通过 `conversation_id` 聚合为同一会话 |

> **【注意】** 首版不实现「个人库 + 共享库混合选题」「跨团队混合问答」等复杂场景。个人问答与团队问答分开展示入口，互不混合数据源。

#### 3.4.2 问答记录管理（自研）

- 自动保存：用户问题、AI 回答、关联知识库 ID 列表、`conversation_id`、提问时间；可选冗余当时分类名称快照

- 团队问答时额外记录 `team_id`（个人问答时为空），便于历史列表区分来源

- 支持按 `conversation_id` 查看同一会话下的多轮记录；支持删除单条、按会话删除、清空全部记录，不支持记录导出

### 3.5 学习笔记模块（全自研）

- 基于前端开源 Markdown 编辑器实现笔记编写，支持标准 Markdown 语法

- 笔记基础操作：新增、编辑、删除、查询

- **多标签**管理：一条笔记可绑定多个标签，存储于 `tags` 字段（建议 JSON 数组，如 `["Java","期末"]`）

- 标签筛选：支持按关键词**模糊匹配**标签（如搜索「Java」可匹配含 `Java`、`JavaWeb` 的标签）

- 笔记为独立功能，不关联文档、不联动 AI，纯手动记录

> **【注意】** 标签筛选为期末项目从简实现，使用 `LIKE` 或 JSON 字符串匹配即可，无需独立标签表。

### 3.6 团队协作与知识库共享模块（全自研）

#### 3.6.1 团队基础管理

- 团队创建者可：创建团队、通过**用户名**精准邀请用户、移除成员、解散团队

- 普通用户可：查看待接受邀请、接受/拒绝邀请、主动退出已加入团队

**邀请与成员流程（采用 `team_member` 单表 + 状态字段，不单独建邀请表）：**

1. 创建团队：写入 `team` 表，同时在 `team_member` 插入创建者记录（`member_role = 创建者`，`status = 已加入`）
2. 邀请成员：按用户名查找用户，插入 `team_member`（`status = 待接受`）
3. 接受邀请：`status` 更新为已加入
4. 拒绝邀请（可选）：`status` 更新为已拒绝，或删除该记录
5. 移除成员 / 退出团队：直接删除对应 `team_member` 记录
6. 解散团队：删除 `team` 及该团队全部 `team_member` 记录

> **【注意】** 不可重复邀请已在团队中或待接受状态的用户；邀请的用户名不存在时返回明确错误提示。

#### 3.6.2 知识库共享规则

- 共享粒度：开启共享后，共享团队创建者（`team.creator_id`）**名下全部** `file_category` 及其下已成功同步的文件

- **不支持**单文件共享、单个分类共享、部分分类共享

- 团队创建者可手动开启/关闭 `team.is_share`；关闭后成员无法预览和问答该创建者的共享内容

- 创建者在共享开启期间**新建的知识库分类**，自动纳入共享范围，无需再次绑定

- 成员权限：仅可在线预览共享文件、发起 AI 问答；不可修改、删除、上传文件，不可管理知识库分类

## 4 非功能性需求

### 4.1 兼容性需求

系统仅适配 PC 端主流浏览器（Chrome、Edge、Firefox 等），无需适配移动端、平板设备，页面布局针对电脑端专属优化。

### 4.2 性能需求

- 面向个人及小型学习团队，无高并发场景，满足单机 Docker 部署、少量用户访问即可

- 文件上传、问答响应延迟在正常网络环境下可接受，无硬性毫秒级指标

### 4.3 易用性需求

- 界面简洁轻量化，符合学生使用习惯，操作逻辑简单直观

- 支持明暗主题切换，适配不同使用场景

- 文件同步成功/失败状态需在列表或详情处清晰展示

### 4.4 安全性需求

- 用户密码加密存储（BCrypt），禁止明文保存

- 基于 JWT + RBAC + 业务校验实现资源隔离

- 用户仅可操作个人资源；团队资源须校验成员身份及共享开关状态

- 禁止越权访问他人文件、笔记、团队数据；MinIO 访问须经后端鉴权或预签名 URL

### 4.5 部署需求

建议 Docker Compose 编排以下服务：

- MySQL
- MinIO
- SpringBoot 后端
- 前端开发期独立运行（Vite Dev Server），生产期可 Nginx 静态部署

## 5 数据库设计需求

系统共 **9 张**核心数据表，结构极简，完全适配 CRUD 开发，具体设计如下。

> **【注意】** 下列字段类型可在实现阶段微调（如 `BIGINT`、`DATETIME`、`JSON`），但语义须与本文档一致。

### 5.1 用户表（user）

| 字段 | 说明 |
|------|------|
| id | 主键 |
| username | 用户名，唯一 |
| password | 加密密码 |
| avatar | 头像 MinIO object key 或 URL |
| theme | 主题模式：light / dark / system |
| create_time | 创建时间 |
| update_time | 更新时间 |

### 5.2 角色表（role）

| 字段 | 说明 |
|------|------|
| id | 主键 |
| role_name | 角色名称：普通用户 / 团队创建者 |
| role_desc | 角色描述 |

预设数据：`1-普通用户`，`2-团队创建者`（ID 仅为示例，以实现为准）。

### 5.3 用户角色关联表（user_role）

| 字段 | 说明 |
|------|------|
| id | 主键 |
| user_id | 用户 ID |
| role_id | 角色 ID |

同一 `user_id` 可有多条记录（如同时拥有普通用户 + 团队创建者）。

### 5.4 知识库分类表（file_category）

| 字段 | 说明 |
|------|------|
| id | 主键 |
| category_name | 知识库分类名称 |
| user_id | 所属用户 ID |
| dify_dataset_id | Dify Dataset ID，首次同步该分类时创建并回填 |
| create_time | 创建时间 |

### 5.5 文件表（file）

| 字段 | 说明 |
|------|------|
| id | 主键 |
| file_name | 文件名称（可重命名） |
| file_type | 文件类型：md / pdf / doc / docx |
| file_path | MinIO object key |
| category_id | 所属知识库分类 ID |
| user_id | 所属用户 ID |
| sync_status | 同步状态：0 未同步 / 1 成功 / 2 失败 |
| dify_document_id | Dify Document ID，同步成功后回填 |
| create_time | 创建时间 |

### 5.6 笔记表（note）

| 字段 | 说明 |
|------|------|
| id | 主键 |
| title | 笔记标题 |
| content | Markdown 内容 |
| tags | 多标签，JSON 数组字符串，如 `["Java","期末"]` |
| user_id | 所属用户 ID |
| create_time | 创建时间 |
| update_time | 更新时间 |

### 5.7 团队表（team）

| 字段 | 说明 |
|------|------|
| id | 主键 |
| team_name | 团队名称 |
| creator_id | 创建者用户 ID |
| is_share | 知识库共享开关：0 关闭 / 1 开启 |
| create_time | 创建时间 |

> **【注意】** 不再使用 `member_ids` 字段存储成员；成员关系统一由 `team_member` 表维护。

### 5.8 团队成员表（team_member）

| 字段 | 说明 |
|------|------|
| id | 主键 |
| team_id | 团队 ID |
| user_id | 用户 ID |
| member_role | 成员角色：0 普通成员 / 1 创建者 |
| status | 状态：0 待接受 / 1 已加入 / 2 已拒绝（2 可选） |
| create_time | 创建时间 |
| update_time | 更新时间 |

唯一约束：`(team_id, user_id)` 不可重复。

### 5.9 问答历史表（chat_history）

| 字段 | 说明 |
|------|------|
| id | 主键 |
| conversation_id | 会话 ID，由 Dify 返回或首轮问答后回填；同一轮多轮对话共用此 ID |
| question | 用户问题 |
| answer | AI 回答 |
| category_ids | 关联知识库分类 ID 列表，**JSON 类型**，如 `[1, 3, 5]` |
| category_names | （可选）提问时分类名称快照，逗号分隔或 JSON，避免分类改名后历史展示异常 |
| team_id | （可选）团队问答时记录团队 ID；个人问答为空 |
| user_id | 提问用户 ID |
| create_time | 提问时间 |

> **【注意】** 每一轮问答存一行；按 `conversation_id` 查询可还原完整多轮会话。`category_ids` 推荐使用 MySQL `JSON` 类型；若数据库版本不支持，可用 `VARCHAR` 存储 JSON 字符串。

## 6 接口整体规划

### 6.1 自研业务接口（SpringBoot 实现）

| 模块 | 主要接口 |
|------|----------|
| 认证 | 注册、登录（返回 JWT）、登出（前端删 Token 即可） |
| 个人中心 | 修改密码、上传头像、修改主题、获取个人信息 |
| 知识库分类 | 分类 CRUD |
| 文件 | 上传（MinIO）、列表、重命名、删除、预览/下载 URL |
| 笔记 | 笔记 CRUD、按标签模糊搜索 |
| 团队 | 创建团队、邀请成员、接受/拒绝邀请、移除成员、退出团队、解散团队、开关共享、成员列表 |
| 问答记录 | 历史列表（支持按 `conversation_id` 聚合）、删除单条、按会话删除、清空 |

所有接口除登录/注册外均需 JWT 鉴权；涉及团队操作的接口须校验 `creator_id` 或成员 `status`。

### 6.2 Dify 对接转发接口（SpringBoot 实现）

| 接口 | 职责 |
|------|------|
| 同步知识库 | 接收 `category_id`，同步该分类下全部 `sync_status` 为 `0` 或 `2` 的文件；调用 Dify 完成 Document 上传与索引，更新 `sync_status` 与 `dify_*_id` |
| 智能问答（个人） | 接收问题、`conversation_id`（可选）、选定的 `category_ids`，组装 `dify_dataset_id` 列表转发 Dify；返回回答、`conversation_id` 并写入 `chat_history` |
| 智能问答（团队） | 接收问题、`conversation_id`（可选）、`team_id`；校验成员与共享开关后，自动使用创建者全部分类的 `dify_dataset_id` 转发 Dify，并写入 `chat_history`（含 `team_id`） |

后端仅做参数校验、权限校验、转发与结果落库，**无核心 AI 逻辑**。

> **【注意】** Dify 应用 API Key、Base URL 等配置放入 `application.yml` 或环境变量，禁止硬编码或提交至版本库。

## 7 项目约束与边界

- 所有 AI 核心能力（文档解析、向量索引、多轮问答、RAG）均由 **Dify 工作流**实现，后端不开发任何 AI 算法逻辑

- 系统仅实现基础 CRUD、JWT 鉴权、RBAC 与 MinIO 存储，不做复杂数据分析、文件在线编辑、问答/笔记导出等冗余功能

- 文件与头像存储于**自建 MinIO**（Docker 部署），不接入公有云对象存储

- 知识库共享仅支持「创建者全部 `file_category` 整体共享」，不支持单文件或单分类精细化权限

- 仅适配 PC 端，无移动端适配需求

- 首版可不实现：Refresh Token、消息通知（邀请仅列表轮询或手动刷新）、同步进度条（仅三态结果展示）

## 8 需求修订记录

| 版本 | 日期 | 修订说明 |
|------|------|----------|
| v1.1 | 2026-05-24 | 明确团队共享粒度、JWT 认证、MinIO 存储、`team_member` 表、Dify 映射规则、`sync_status` 三态、多标签笔记、`category_ids` JSON 存储等 |
| v1.2 | 2026-05-24 | 确定同步范围为按单分类同步；团队问答固定使用创建者全部分类；新增 `conversation_id` 多轮会话存储规则及 `chat_history.team_id` |
