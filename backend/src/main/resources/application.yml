server:
  port: 8080

#  数据库连接
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/数据库名?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=utf8
    username: root
    password: 123456

  #  JPA 自动建表
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true